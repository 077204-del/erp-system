# Backend API base URL only (Render Express).
# Copy to .env.production before `npm run build` if needed.
# (otherwise the build uses DEFAULT_API_ORIGIN in src/config/apiBase.js).
REACT_APP_API_URL=https://erp-system-1-rgd2.onrender.com
