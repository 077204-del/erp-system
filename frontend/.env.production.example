# Copy to .env.production before `npm run build` if the APK should call a fixed server
# (otherwise the build uses the LAN default from src/config/apiBase.js).
# REACT_APP_API_URL=https://api.example.com
